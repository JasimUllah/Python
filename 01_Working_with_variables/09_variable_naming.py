# Rules for naming variables
# 1. To make the code readable
# 2. You can have multiple word to name a vraible but you can;t have space in between them
# 3. You can't use numbers in strating of the variable
# 4. Don't use the variable names as the function names

name = input("What is your name? ")
length = len(name)
print(length)